# Allow password authentication for all users (local connections)
local   all             all                                     md5

# Allow password authentication for all users from localhost (127.0.0.1)
host    all             all             127.0.0.1/32            md5

# Allow password authentication for all users from localhost (IPv6)
host    all             all             ::1/128                 md5

# Allow replication connections (local)
local   replication     all                                     md5

# Allow replication connections from localhost (IPv4)
host    replication     all             127.0.0.1/32            md5

# Allow replication connections from localhost (IPv6)
host    replication     all             ::1/128                 md5

# Allow password authentication for all users from any host (0.0.0.0/0)
host    all             all             0.0.0.0/0               md5
